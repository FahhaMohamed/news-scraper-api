{
  "name": "web-scrap-breaking-news",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx nodemon index.js",
    "dev": "set NODE_ENV=development && npx nodemon index.js",
    "prod": "set NODE_ENV=production && npx nodemon index.js"
  },
  "author": "FAHHAM MSM",
  "license": "ISC",
  "description": "get the news data by web scrapping and create a rest api for show that data",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "puppeteer": "^23.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
